BASE_TEXT_MESSAGE=Extract all calendar events from these images and generate a valid ICS file. Use time zone: ${timeZone}. Include all required ICS fields like UID, DTSTAMP, TZID, etc. Only output the raw ICS content, no additional text. NEVER include any additional text or formatting in your response, not even a comment or backticks.
PROMPT=You are a calendar expert. Generate a complete ICS file from images. Only return ICS content, no extra text.
